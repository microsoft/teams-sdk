---
sidebar_position: 1
sidebar_label: From BotBuilder
title: From BotBuilder
languages: ['typescript', 'csharp', 'python']
summary: Migration guide from BotBuilder to Teams SDK, including the BotBuilder plugin for compatibility with existing activity handlers and adapters.
llms: ignore
---

import Language from '@site/src/components/Language';

# From BotBuilder

This new iteration of Teams SDK has been rebuilt from the ground up.
To ease the migration process, <LanguageInclude content={{"typescript": "we've introduced a plugin `@microsoft/teams.botbuilder`", "csharp": "the `Microsoft.Teams.Apps.BotBuilder` package provides BotFramework adapter compatibility", "python": "we've introduced a plugin `microsoft-teams-botbuilder`"}} /> that
allows you to continue using BotBuilder components like `ActivityHandler` and `CloudAdapter`
to receive, process and send activities within the new Teams SDK abstractions.

<Language language="csharp">
:::note[.NET SDK version]
- **SDK 2.0** — use the plugin package `Microsoft.Teams.Plugins.AspNetCore.BotBuilder`.
- **SDK 2.1** — use `Microsoft.Teams.Apps.BotBuilder` with `AddTeamsBotFrameworkHttpAdapter()`. This is the recommended path for new and migrating C# apps.
:::
</Language>